package org.example.analyzer.spoon.implicit;

/**
 * @deprecated Split into {@link MapFactCollector} (Phase 1) and {@link BridgeDetector} (Phase 2).
 *   Implementations that only participated in one phase no longer need to implement empty stubs.
 *   This interface will be removed in a future cleanup.
 */
@Deprecated
public interface KeyUsagePattern {
}
